# Curriculum — [Subject]

> Plan a subject with the tutor ("help me plan a curriculum for [X]"), then save it to
> `subjects/<subject>/curriculum.md`. The tutor reads and updates this file directly —
> it ticks boxes and edits `status:` as you learn. You don't hand-maintain it.

## Why I'm learning this
[Motivation / how I'll use it — keeps sessions goal-anchored]

## Target outcome
[What "I know this" looks like concretely — e.g. "can design and defend X", "pass Y exam", "ship Z"]

## Level hypothesis (the tutor verifies this — it does not trust it)
[Rough guess, e.g. "comfortable coding, rusty on graphs". The tutor probes and recalibrates.]

## Placement verdict (the tutor writes this)
_Not yet placed. On first session the tutor runs placement and records the result here._
<!-- e.g. "Placed at topic 8 (2026-07-01); 1–7 known except 5 (shaky). User confirmed." -->

## Topics
> Ordered foundational → advanced. Each line: `id · title` — description `deps:` `status:`.
> **deps** = ids this topic builds on (omit / `none` if independent — enables per-track placement).
> **status** ∈ `unknown | pass (date) | shaky | deferred | fail | stale`. Box checked `[x]` only on `pass`.

- [ ] **1 · [Topic]** — [description]. `deps: none` `status: unknown`
- [ ] **2 · [Topic]** — [description]. `deps: 1` `status: unknown`
- [ ] **3 · [Topic]** — [description]. `deps: 1` `status: unknown`

## Milestones
Concrete, demonstrable checkpoints.
- [ ] …
- [ ] …

## Resources
[Books, docs, courses, links]

## Constraints / notes
- Pace: [e.g. "2 sessions/week, ~45 min each."]
- Revisit shaky/deferred items (see `index.md`) at the start of each session.
